# These are options for unit tests

# Configure baseline
rule:
  include:
  - rule1
  - rule2
  exclude:
  - rule3
  - rule4

# Configure binding
binding:
  targetName:
  - ResourceName
  - AlternateName
  targetType:
  - ResourceType
  - kind
