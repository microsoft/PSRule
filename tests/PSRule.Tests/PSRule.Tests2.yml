# These are options for unit tests

# Configure binding
binding:
  targetName:
  - ResourceName
  - AlternateName
  targetType:
  - ResourceType
  - kind

# Configure input
input:
  targetType:
  - virtualMachine
  - virtualNetwork

# Configure output
output:
  culture:
  - 'en-CC'
  - 'en-DD'

# Configure baseline
rule:
  include:
  - rule1
  - rule2
  exclude:
  - rule3
  - rule4
