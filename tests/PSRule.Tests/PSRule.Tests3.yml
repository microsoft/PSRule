# These are options for unit tests

# Configure baseline
baseline:
  ruleName: [ 'rule1' ]
  exclude: [ 'rule3' ]

# Configure binding
binding:
  targetName: [ 'ResourceName' ]
  targetType: [ 'ResourceType' ]
